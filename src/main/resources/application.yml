spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/DatabaseTrafficPoliceman
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
springdoc:
  swagger-ui:
    path: /documentation
server:
  servlet:
    context-path: /number